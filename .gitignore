# ── Private guest data ──────────────────────────────────────────
# Never commit raw guest lists — they contain personal information.
media/*.csv

# Node modules (if ever installed locally)
node_modules/
